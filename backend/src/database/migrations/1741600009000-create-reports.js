// src/database/migrations/1741600009000-create-reports.js

/**
 * TASK-015: Create reports table
 * - AI-generated weekly progress reports
 * - FK to users with CASCADE delete (COPPA compliance)
 * - Composite index (child_id, period_start DESC) for latest report lookup
 */
module.exports = class CreateReports1741600009000 {
  name = 'CreateReports1741600009000';

  async up(queryRunner) {
    await queryRunner.query(`
      CREATE TABLE reports (
        id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
        child_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
        report_type VARCHAR(20) DEFAULT 'WEEKLY',
        content TEXT NOT NULL,
        ai_model VARCHAR(50),
        ai_disclaimer TEXT DEFAULT 'This report was generated by AI and should be reviewed by a professional.',
        period_start DATE NOT NULL,
        period_end DATE NOT NULL,
        created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
      );
    `);

    await queryRunner.query(`
      CREATE INDEX idx_reports_child_period
        ON reports (child_id, period_start DESC);
    `);
  }

  async down(queryRunner) {
    await queryRunner.query(`DROP TABLE IF EXISTS reports CASCADE;`);
  }
};
